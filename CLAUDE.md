# 🛑 СТОП! ПРОЧИТАЙ ПЕРЕД ЛЮБЫМ ДЕЙСТВИЕМ

## ❌ ЗАПРЕЩЕНО (нарушение = потеря времени пользователя):

| Ситуация | ЗАПРЕЩЕНО | ОБЯЗАТЕЛЬНО |
|----------|-----------|-------------|
| Ошибка/баг/"не работает" | Угадывать причину | `systematic-debugging` НЕМЕДЛЕННО |
| UI/компонент/страница | Писать код сразу | `frontend-design` НЕМЕДЛЕННО |
| Новая фича/функция | Кодить по памяти | Exa best practices СНАЧАЛА |
| API/бэкенд архитектура | Делать "как обычно" | Предложи `backend-architect` |
| Медленные запросы БД | Добавлять индексы наугад | Предложи `database-optimization` |

**Даже если причина "очевидна" — skill/агент обязателен. БЕЗ ИСКЛЮЧЕНИЙ.**

---

Всегда отвечай на русском языке.
Используй Context7 MCP для документации библиотек/API без явной просьбы пользователя.

---

## 🔧 Skills (автоматический вызов)

### 🔴 Process Skills (вызывай ПЕРВЫМИ)

| Skill | Триггеры | Анти-паттерн |
|-------|----------|--------------|
| `systematic-debugging` | "не работает", ошибка, баг, 500/404, TypeError, stack trace | ❌ Угадывать причину |
| `brainstorming` | Новая фича, творческая работа, изменение поведения | ❌ Сразу кодить |
| `test-driven-development` | Реализация фичи/багфикса (ПЕРЕД кодом) | ❌ Код без тестов |
| `writing-plans` | Мультишаговая задача, спецификация, требования | ❌ Делать без плана |

### 🟢 Implementation Skills

| Skill | Триггеры |
|-------|----------|
| `frontend-design` | UI, компонент, страница, дизайн, интерфейс |
| `ui-ux-pro-max` | Детальный дизайн, палитры, типографика, 50+ стилей |
| `git-workflow-manager` | Commit, release, changelog, версионирование |
| `claude-md-writer` | Создание/рефакторинг CLAUDE.md файлов |
| `kie-ai` | Упоминание kie, seedream, veo, suno, runway, kling, flux |
| `polza-ai` | Упоминание polza, OpenAI-совместимый API |

### 🔄 Workflow Skills (автопереходы)

| Skill | Когда вызывать |
|-------|----------------|
| `using-git-worktrees` | Нужна изоляция от текущего workspace |
| `subagent-driven-development` | Выполнение плана с review в текущей сессии |
| `executing-plans` | Выполнение плана в отдельной сессии |
| `verification-before-completion` | ПЕРЕД заявлением "готово"/"работает" |
| `finishing-a-development-branch` | Реализация завершена, нужно интегрировать |
| `dispatching-parallel-agents` | 3+ независимых задач/ошибок в разных файлах |

### 👀 Review Skills

| Skill | Триггер |
|-------|---------|
| `requesting-code-review` | После завершения задачи, перед merge/PR |
| `receiving-code-review` | Получен feedback на код (перед implement) |

### 📊 Flow: Полный цикл разработки

```
brainstorming → writing-plans → [worktree?] → subagent/executing-plans
                                                        ↓
                                    verification-before-completion
                                                        ↓
                                    finishing-a-development-branch
```

**Приоритет:** Process Skills → Implementation Skills → Workflow Skills

---

## 🤖 Агенты (предлагай с подтверждением)

| Агент | Триггеры | Формат |
|-------|----------|--------|
| `debugger` | После systematic-debugging, сложные баги | "💡 Рекомендую: `debugger`" |
| `backend-architect` | API, OAuth, JWT, микросервисы | "💡 Рекомендую: `backend-architect`" |
| `database-optimization` | EXPLAIN, индексы, медленные запросы | "💡 Рекомендую: `database-optimization`" |
| `code-reviewer` | Завершён этап, перед PR | "💡 Рекомендую: `code-reviewer`" |
| `code-simplifier` | Рефакторинг, дублирование кода | "💡 Рекомендую: `code-simplifier`" |

---

## 🔌 MCP серверы

### Автоматическое использование:

| MCP | Триггер | Действие |
|-----|---------|----------|
| **Exa** | Новая фича, best practices | `get_code_context_exa` СНАЧАЛА |
| **Context7** | Документация библиотеки | `query-docs` для синтаксиса |
| **Ref** | Чтение URL документации | `ref_read_url` |
| **shadcn** | UI компонент | Поиск готового компонента СНАЧАЛА |
| **Playwright** | Проверка сайта, тестирование | См. ниже |
| **Supabase** | Работа с Supabase | Используй MCP вместо ручных запросов |

### Приоритет для документации:
1. Context7 (структурированная) → 2. Ref (конкретный URL) → 3. Exa (примеры/issues)

### Приоритет для UI:
1. shadcn (готовые компоненты) → 2. frontend-design skill

### Алгоритм для новой фичи:
```
1. Пользователь просит фичу → НЕ пиши код!
2. Exa: "[фича] best practices 2025 [технология] production"
3. Покажи 2-3 подхода → пользователь выбирает
4. Только потом → пиши код
```

### 🎭 Playwright MCP

| Ситуация | Действие |
|----------|----------|
| "Посмотри сайт/страницу" | `browser_navigate` + `browser_snapshot` |
| "Проверь как выглядит" | `browser_take_screenshot` |
| После деплоя/изменений UI | Предложи: "💡 Проверить страницу через Playwright?" |
| "Заполни форму" | `browser_fill_form` |
| "Проверь ошибки в консоли" | `browser_console_messages` |
| E2E тестирование | Полный flow через Playwright |

**snapshot vs screenshot:**
- `browser_snapshot` — для взаимодействия (видит элементы, может кликать)
- `browser_take_screenshot` — для сохранения изображения

**Исключения (MCP не нужен):** опечатки, форматирование, повторение паттерна из проекта.

---

## 📌 Технические правила

### Vercel + Vite: Избегай bracket-синтаксис

При `framework: "vite"` роуты `[param].ts` могут не работать (405).

**Решение — URL parsing:**
```typescript
// api/auth.ts (без brackets)
const endpoint = url.pathname.split('/').filter(Boolean).pop();
switch (endpoint) {
  case 'login': return handleLogin(req, res);
  case 'register': return handleRegister(req, res);
}
```

```json
// vercel.json
{ "rewrites": [{ "source": "/api/auth/:path*", "destination": "/api/auth" }] }
```

### Express.js: req.query read-only

```javascript
// ❌ Object.assign(req.query, params) — не работает
// ✅ Object.defineProperty(req, 'query', { value: {...req.query, ...params} })
```

### Лимиты платформ

- **Vercel Hobby:** 12 serverless функций
- **Vercel Pro:** без лимита
- **Netlify Free:** 125k функций/месяц

### HTTP заголовки: только ASCII

```typescript
// ❌ headers: { 'X-Title': 'Суть.' }  — crash на сервере (код символа > 255)
// ✅ headers: { 'X-Title': 'Sut App' }  — только ASCII
```

### LLM JSON ответы: очищай markdown

LLM часто возвращают JSON в markdown-обёртке: ` ```json {...} ``` `

```typescript
let clean = content.trim();
if (clean.startsWith('```json')) clean = clean.slice(7);
if (clean.startsWith('```')) clean = clean.slice(3);
if (clean.endsWith('```')) clean = clean.slice(0, -3);
const result = JSON.parse(clean.trim());
```

### LLM инструкции: не позволяй клиенту переопределять формат

Клиентский config не должен полностью заменять системные инструкции — критические части (формат JSON ответа) должны быть обязательными.

```typescript
// ❌ const instruction = config?.instruction || defaultInstruction;
// ✅ const instruction = `${config?.instruction || baseInstruction}\n\n${REQUIRED_FORMAT}`;
```

---

## 🔐 Авторизация

Передавай токены во ВСЕХ компонентах (админ, пользователь, публичные).

```typescript
// fetchWithAuth.ts
export async function fetchWithAuthGet(url: string) {
  const token = localStorage.getItem('token');
  const res = await fetch(url, {
    headers: { 'Authorization': `Bearer ${token}` }
  });
  if (res.status === 401) window.dispatchEvent(new CustomEvent('auth:expired'));
  return res.json();
}
```

---

## 🔗 Интеграция с внешними сервисами

**Чеклист ДО деплоя:**
- [ ] Проверил типы через Context7
- [ ] Добавил env variables (локально + production)
- [ ] Добавил логирование ошибок

```typescript
try {
  const result = await externalAPI.call();
} catch (error) {
  console.error('API error:', { service: 'X', error: error.message });
  return res.status(500).json({ error: error.message });
}
```

---

## 📝 Критические требования

Когда пользователь говорит "критически важно" — держи это в фокусе всю сессию.
Перед каждым изменением проверяй: не сломает ли это критическое требование?

---

## 🔄 Авто-синхронизация claude-config

После любого редактирования файлов в `~/claude-config/` — **автоматически коммить и пушь**:

```bash
cd ~/claude-config && git add . && git commit -m "Update: краткое описание изменений" && git push
```

Триггеры:
- Редактирование `CLAUDE.md`, `agent.md`
- Изменение скиллов в `skills/`
- Изменение агентов в `agents/`

Сообщение коммита должно кратко описывать что изменилось.
